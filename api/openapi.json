{
  "openapi": "3.0.3",
  "info": {
    "title": "compy",
    "description": "Local OpenTelemetry Collector manager: REST API served by whatever UI process is running (compy ui / window / tray). Localhost-only; see internal/webui's host-check + CSRF middleware.",
    "version": "3.0.0"
  },
  "servers": [
    { "url": "http://127.0.0.1:8080" }
  ],
  "components": {
    "schemas": {
      "Error": {
        "type": "object",
        "properties": {
          "error": { "type": "string" },
          "still_running": {
            "type": "string",
            "description": "Present when an activation failed and the previous configuration was put back, e.g. \"otlp-to-bronto · staging\"."
          }
        },
        "required": ["error"]
      },
      "Status": {
        "type": "object",
        "properties": {
          "running": { "type": "boolean" },
          "distro": { "type": "string" },
          "grpc_port": { "type": "integer" },
          "http_port": { "type": "integer" },
          "protocol": {
            "type": "string",
            "enum": ["grpc", "http/protobuf", "http/json"],
            "description": "The advertised OTLP protocol; endpoint points at the port it uses (the gRPC port for grpc, the HTTP port otherwise)."
          },
          "endpoint": { "type": "string" },
          "config": { "type": "string" },
          "preset": { "type": "string" },
          "os_env": { "type": "boolean" },
          "recent": {
            "type": "array",
            "description": "Configurations that have run, most recent first (the menu bar's ordering).",
            "items": { "type": "string" }
          },
          "listening": {
            "type": "array",
            "description": "TCP ports the collector process is actually listening on, detected from the OS (launchd pid + lsof), sorted ascending. Absent when stopped or undetectable — never derived from settings or YAML.",
            "items": { "type": "integer" }
          },
          "conformance": {
            "$ref": "#/components/schemas/PortsVerdict",
            "description": "Whether an app following compy's advertised endpoint would reach the running collector. Absent when stopped or when port detection is unavailable — no detection, no claim."
          }
        }
      },
      "PortsVerdict": {
        "type": "object",
        "description": "The advertised-ports verdict. conforming rides on the primary port alone — the port OTEL_EXPORTER_OTLP_ENDPOINT actually uses under the advertised protocol (the HTTP port for http/protobuf and http/json, the gRPC port for grpc); the other port missing is reported but never fails the verdict by itself.",
        "properties": {
          "conforming": { "type": "boolean" },
          "missing_http": { "type": "boolean", "description": "The advertised HTTP port is not among the detected listeners (a softer addendum when the advertised protocol is grpc)." },
          "missing_grpc": { "type": "boolean", "description": "The advertised gRPC port is not among the detected listeners (a softer addendum unless the advertised protocol is grpc)." },
          "actual": {
            "type": "array",
            "description": "Detected non-telemetry listeners — the ports POST /api/service/adopt-ports would adopt.",
            "items": { "type": "integer" }
          }
        },
        "required": ["conforming", "missing_http", "missing_grpc"]
      },
      "Settings": {
        "type": "object",
        "properties": {
          "grpc_port": { "type": "integer" },
          "http_port": { "type": "integer" },
          "protocol": {
            "type": "string",
            "enum": ["grpc", "http/protobuf", "http/json"],
            "description": "The advertised OTLP protocol (default http/protobuf)."
          }
        }
      },
      "SettingsUpdate": {
        "type": "object",
        "description": "Partial: an absent field leaves that setting unchanged. Port changes take effect on next apply; a protocol change is advertisement-only and needs no restart.",
        "properties": {
          "grpc_port": { "type": "integer" },
          "http_port": { "type": "integer" },
          "protocol": {
            "type": "string",
            "enum": ["grpc", "http/protobuf", "http/json"],
            "description": "What the advertised endpoint speaks. Any other value is a 400."
          }
        }
      },
      "Var": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "default": { "type": "string" },
          "has_default": { "type": "boolean" },
          "description": { "type": "string" }
        }
      },
      "Info": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "provenance": { "type": "string", "enum": ["shipped", "remote", "local"] },
          "modified": { "type": "boolean" },
          "meta": {
            "type": "object",
            "properties": {
              "remote_url": { "type": "string" },
              "pristine_sha256": { "type": "string" },
              "presets": {
                "type": "object",
                "additionalProperties": {
                  "type": "object",
                  "additionalProperties": { "type": "string" }
                }
              },
              "active_preset": { "type": "string" }
            }
          },
          "vars": {
            "type": "array",
            "items": { "$ref": "#/components/schemas/Var" }
          }
        }
      },
      "ConfigDetail": {
        "type": "object",
        "properties": {
          "info": { "$ref": "#/components/schemas/Info" },
          "yaml": { "type": "string" }
        }
      },
      "Distro": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "path": { "type": "string" },
          "version": { "type": "string", "description": "The INSTALLED version: a downloaded definition's version in effect (compiled-in pin or pulled update), the bundled collector's build stamp (\"unknown\" when the stamp file is missing). Empty for user-managed entries, for the bundled collector when it is not built, and for undownloaded definitions (nothing is installed — see fetch_version for what a download would fetch)." },
          "selected": { "type": "boolean" },
          "definition": { "type": "boolean" },
          "bundled": { "type": "boolean", "description": "True for the collector shipped with compy (otelcol-compy, resolved next to the compy executable, never downloaded — it updates with compy releases), unless a user entry overrides the name." },
          "available": { "type": "boolean" },
          "downloaded": { "type": "boolean" },
          "user_entry": {
            "type": "boolean",
            "description": "True iff this name has an actual entry in the user distro registry (distros.json) — a custom distro, or an override of a shipped definition's path. False for a shipped definition that's merely been downloaded to its default path: there's no registry entry for DELETE /api/distros/{name} to remove."
          },
          "download": {
            "$ref": "#/components/schemas/DownloadProgress",
            "description": "Present only while a download of this distro is in flight in the serving process — including one an activation started automatically (the default collector fetched on first need), so the settings screen's periodic refresh can show a progress bar for a download it did not start."
          },
          "latest_available": {
            "type": "string",
            "description": "The newest upstream release, present only when it is newer than this row's INSTALLED version, per the persisted release check (the tray's ~12h background check, or an on-demand GET /api/distros/{name}/update — both record the same result). Read from disk, never fetched during this request. Only on installed updatable rows: never on an undownloaded definition (nothing exists to update — its first download fetches the newest release directly), the bundled collector (it updates with compy releases), or user-managed entries, and a malformed/unknown current version never claims."
          },
          "fetch_version": {
            "type": "string",
            "description": "What a download of this row would fetch: the newest release the persisted check knows, or the compiled-in pin when no check has run yet (fetch_pinned). Only on undownloaded, platform-available definitions."
          },
          "fetch_pinned": {
            "type": "boolean",
            "description": "Present (true) when fetch_version is the compiled-in pin because no release check result exists yet, rather than a checked latest release."
          },
          "checked_at": {
            "type": "string",
            "format": "date-time",
            "description": "When the persisted release check last succeeded (a failed check keeps the previous result and its checked_at rather than claim staleness away). Absent until a check has succeeded; only on updatable rows, like latest_available."
          }
        }
      },
      "Health": {
        "type": "object",
        "properties": {
          "available": { "type": "boolean" },
          "port": { "type": "integer", "description": "The localhost port whose /metrics answered this scrape. Absent when unavailable." },
          "received": { "type": "integer" },
          "exported": { "type": "integer" },
          "queue": { "type": "integer" },
          "dropped": { "type": "integer" },
          "dropping": {
            "type": "object",
            "description": "The drop diagnosis: present only when the running collector reports dropped > 0 AND the active configuration's effective preset is missing required values (cfgstore.MissingRequired — no ${VAR:-fallback} in the yaml, not COMPY_*, empty in the preset). Drops with all values present carry no diagnosis: some other cause, no vars blamed.",
            "properties": {
              "vars": { "type": "array", "items": { "type": "string" } }
            },
            "required": ["vars"]
          }
        },
        "required": ["available"]
      },
      "DownloadProgress": {
        "type": "object",
        "properties": {
          "status": { "type": "string", "enum": ["idle", "downloading", "done", "failed"] },
          "pct": { "type": "integer" },
          "error": { "type": "string" }
        },
        "required": ["status", "pct"]
      },
      "Ok": {
        "type": "object",
        "properties": { "ok": { "type": "boolean" } }
      }
    }
  },
  "paths": {
    "/api/status": {
      "get": {
        "summary": "Service state, distro, ports, active config",
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Status" } } }
          }
        }
      }
    },
    "/api/log": {
      "get": {
        "summary": "Tail of the collector log",
        "parameters": [
          {
            "name": "lines",
            "in": "query",
            "required": false,
            "description": "Number of lines (default 50, clamped to 2000)",
            "schema": { "type": "integer" }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": { "log": { "type": "string" } }
                }
              }
            }
          },
          "400": {
            "description": "Invalid lines value",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/env": {
      "get": {
        "summary": "OTEL_* environment variables compy exposes",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "vars": { "type": "object", "additionalProperties": { "type": "string" } },
                    "script": { "type": "string" }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/os-env": {
      "post": {
        "summary": "Enable/disable OS-level (launchctl setenv) injection",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": { "on": { "type": "boolean" } },
                "required": ["on"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/settings": {
      "get": {
        "summary": "Current settings",
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Settings" } } }
          }
        }
      },
      "put": {
        "summary": "Update settings (partial)",
        "requestBody": {
          "content": {
            "application/json": { "schema": { "$ref": "#/components/schemas/SettingsUpdate" } }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Settings" } } }
          },
          "400": {
            "description": "Malformed body",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/collector/health": {
      "get": {
        "summary": "The collector's own metrics, scraped from its telemetry endpoint",
        "description": "Four numbers for the Collector screen. localhost:8888 (otelcol's default) is tried first; if it does not answer, the collector's detected listening ports are probed and the first parsable /metrics wins — `port` names the winner. A stopped or unscrapable collector answers 200 with available:false and zeroes, never an error. Sources (otelcol 0.135, counters matched with or without a _total suffix): received = otelcol_receiver_accepted_{spans,metric_points,log_records}; exported = otelcol_exporter_sent_{spans,metric_points,log_records}; queue = otelcol_exporter_queue_size; dropped = otelcol_receiver_refused_*, otelcol_exporter_send_failed_*, otelcol_exporter_enqueue_failed_*.",
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Health" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/service/adopt-ports": {
      "post": {
        "summary": "Set the advertised grpc/http ports to the running config's actual OTLP listeners",
        "description": "The advertisement follows the deliberate config. With an empty body the detected non-telemetry listeners are classified (the otlp/http listener speaks HTTP/1.1, the grpc one does not); any ambiguity — no or several http-ish candidates, more candidates than slots — is refused with a 400 naming the candidates, never guessed silently. An explicit body resolves it: each given port must be one of the detected candidates. Applying goes through the same path as PUT /api/settings (OS-level env refreshed when on); shipped configs pick the new ports up on their next activation.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "description": "Optional. Absent fields are classified (or, when the other is given, left unchanged).",
                "properties": {
                  "grpc_port": { "type": "integer" },
                  "http_port": { "type": "integer" }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK — the resulting settings",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Settings" } } }
          },
          "400": {
            "description": "Collector not running / nothing detected, classification ambiguous (candidates named in the message), or an explicit port that is not a detected listener",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/service/apply": {
      "post": {
        "summary": "Re-activate the current configuration and set",
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/service/stop": {
      "post": {
        "summary": "Stop the collector (the active configuration stays named)",
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/service/start": {
      "post": {
        "summary": "Run the active configuration again",
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 no active configuration, or one the collector rejects",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error \u2014 the collector did not come up; \"still_running\" names what was put back",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/service/validate": {
      "post": {
        "summary": "Validate the active configuration against its distro without touching the running service",
        "responses": {
          "200": {
            "description": "Valid",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": { "ok": { "type": "boolean" } }
                }
              }
            }
          },
          "500": {
            "description": "Invalid (the collector's own diagnostics)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/factory-reset": {
      "post": {
        "summary": "Reset compy to factory settings: uninstall the collector job, wipe the state directory's contents, and re-create the shipped defaults (the tray's own agent is untouched)",
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs": {
      "get": {
        "summary": "List configurations",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": { "type": "array", "items": { "$ref": "#/components/schemas/Info" } }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Create a configuration (blank template if yaml is absent)",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "yaml": { "type": "string" }
                },
                "required": ["name"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Malformed body",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/from-url": {
      "post": {
        "summary": "Create a configuration from a remote YAML URL",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "url": { "type": "string" }
                },
                "required": ["name", "url"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Malformed body",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}": {
      "get": {
        "summary": "One configuration's info and YAML",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/ConfigDetail" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      },
      "delete": {
        "summary": "Delete a configuration (the active one may not be deleted)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/yaml": {
      "put": {
        "summary": "Replace a configuration's YAML",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": { "text/plain": { "schema": { "type": "string" } } }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/meta": {
      "put": {
        "summary": "Update a configuration's metadata (partial)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "remote_url": { "type": "string" }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Malformed body",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/copy": {
      "post": {
        "summary": "Duplicate a configuration under a new name",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": { "dst": { "type": "string" } },
                "required": ["dst"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Malformed body",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/activate": {
      "post": {
        "summary": "Make this configuration the running one",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": { "preset": { "type": "string" } }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/validate": {
      "post": {
        "summary": "Validate this configuration against its own distro, using its own active set's environment (any config, not just the active one)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "Valid",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": { "ok": { "type": "boolean" } }
                }
              }
            }
          },
          "500": {
            "description": "Invalid (the collector's own diagnostics, verbatim)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/sync": {
      "post": {
        "summary": "Refetch a remote configuration (refuses if locally modified)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/resync": {
      "post": {
        "summary": "Refetch a remote configuration, discarding local edits",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/reset": {
      "post": {
        "summary": "Restore a modified built-in configuration to its shipped version (presets kept; the running configuration is re-applied)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request — a caller mistake (invalid name, unknown configuration, a configuration that is not a built-in, or one already matching the shipped version)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/rename": {
      "post": {
        "summary": "Rename a configuration (the active configuration follows the rename; a running one is re-applied)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": { "to": { "type": "string" } },
                "required": ["to"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request — a malformed body, an invalid or unknown name, or a target name that already exists",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/sync-all": {
      "post": {
        "summary": "Sync every unmodified remote configuration",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "synced": { "type": "array", "items": { "type": "string" } }
                  }
                }
              }
            }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/presets/{preset}": {
      "put": {
        "summary": "Create or replace a whole preset",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "preset", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "values": { "type": "object", "additionalProperties": { "type": "string" } }
                },
                "required": ["values"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Malformed body",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      },
      "delete": {
        "summary": "Delete a preset (never the last one, nor the active one)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "preset", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/presets/{preset}/use": {
      "post": {
        "summary": "Make this preset the configuration's active one",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "preset", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/configs/{name}/presets/{preset}/rename": {
      "post": {
        "summary": "Rename this preset (the active preset follows the rename)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "preset", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": { "to": { "type": "string" } },
                "required": ["to"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Malformed body",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error (preset missing, or the new name already exists)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/distros": {
      "get": {
        "summary": "List the distro registry",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": { "type": "array", "items": { "$ref": "#/components/schemas/Distro" } }
              }
            }
          }
        }
      },
      "post": {
        "summary": "Register a user distro",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "path": { "type": "string" }
                },
                "required": ["name", "path"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK (warning set when this overrides a shipped definition)",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": { "warning": { "type": "string" } }
                }
              }
            }
          },
          "400": {
            "description": "Malformed body",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/distros/{name}": {
      "put": {
        "summary": "Register or update a user distro's binary path (path must exist and be executable)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": { "path": { "type": "string" } },
                "required": ["path"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK (warning set when this overrides a shipped definition)",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": { "warning": { "type": "string" } }
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, invalid distro name, or path missing/not executable",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "413": {
            "description": "Body too large",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      },
      "delete": {
        "summary": "Remove a user distro entry (an override of a shipped definition reverts to it instead of disappearing; see \"reverted\")",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": { "reverted": { "type": "boolean" } }
                }
              }
            }
          },
          "400": {
            "description": "The selected distro, or a pure definition name with no user entry",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/distros/{name}/use": {
      "post": {
        "summary": "Select the global default distro",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Ok" } } }
          },
          "400": {
            "description": "Bad request \u2014 a caller mistake (invalid name, unknown configuration or preset, a configuration the collector rejects, a binary that isn't there)",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/distros/{name}/progress": {
      "get": {
        "summary": "How this collector binary's download is going",
        "description": "Poll after POST .../fetch, which returns immediately. status is idle | downloading | done | failed; pct is 0-100 (and stays put when the server declares no Content-Length); error is present only when status is failed. Progress is per-process and in memory: a name nobody has fetched since this process started reads idle, whether or not the binary is on disk.",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/DownloadProgress" } } }
          },
          "500": {
            "description": "Error",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/distros/{name}/fetch": {
      "post": {
        "summary": "Start downloading a shipped distro definition's binary (returns at once; poll .../progress)",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "Accepted \u2014 the download has started (or the binary was already present). Nothing else is reported here: an unknown name, a checksum mismatch or a network failure all surface through .../progress as status \"failed\", since this request is long gone by then."
          }
        }
      }
    },
    "/api/distros/{name}/update": {
      "get": {
        "summary": "Check the latest upstream release for a pinned distro (core, contrib, otlp) \u2014 on demand, downloads nothing",
        "description": "The version in effect and the newest upstream release of open-telemetry/opentelemetry-collector-releases. A successful check is recorded in the state dir (the same record the tray's background check writes), so every distro row's latest_available/checked_at refreshes with it. 400 for the bundled collector (it updates with compy releases), a user-managed entry, an undownloaded distro (nothing installed to update — a download fetches the newest release directly), or an unknown name; a network failure or rate limit is an honest error, never a claim about versions.",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "current": { "type": "string" },
                    "latest": { "type": "string" }
                  }
                }
              }
            }
          },
          "400": { "description": "Not updatable: the bundled collector, a user-managed entry, or an unknown name", "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } } },
          "500": { "description": "Error", "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } } }
        }
      },
      "post": {
        "summary": "Pull the latest upstream release of a pinned distro (returns at once when newer; poll .../progress)",
        "description": "Checks synchronously; started false with current == latest means already newest (nothing to do). Otherwise the tarball downloads in the background \u2014 verified against the .sha256 checksum asset published in the same upstream release \u2014 into its own versioned directory, becomes the version in effect, and, when this distro is the collector in use and the job is running, the service re-applies onto it (a new collector that fails to start rolls the update back with the last-good restore; a stopped collector stays stopped).",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "current": { "type": "string" },
                    "latest": { "type": "string" },
                    "started": { "type": "boolean" }
                  }
                }
              }
            }
          },
          "400": { "description": "Not updatable: the bundled collector, a user-managed entry, or an unknown name", "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } } },
          "500": { "description": "Error", "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } } }
        }
      }
    }
  }
}
